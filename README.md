# 형님 대시보드 (GitHub Pages)

형님 일정/패턴/일일 기록을 **로컬 우선(localStorage)** 으로 관리하는 대시보드입니다.

## Privacy
- 기본 동작은 브라우저에만 저장됩니다.
- GitHub Pages는 정적 호스팅이라 서버 DB가 없습니다.
- 원하면 `Export JSON`으로 백업하고, `Import`로 복구합니다.

## Data sources
- loopStateUrl: YoMi의 자기개선/코칭 로그(`lifecoach-state.json`)를 읽어와 패턴 분석에 사용합니다.
